wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh
bash
conda config --set auto_activate_base false
conda create -n propagation -y
conda activate propagation
conda config --env --add channels conda-forge
conda install python pandas numpy scipy numba bayesian-optimization frozendict -y

# Either use OpenMPI from conda:
# conda install openmpi mpi4py

# Or the native MPI distribution:
# pip install mpi4py

# Install optional packages
# conda install jupyterlab profilehooks networkx matplotlib black git plotnine -y