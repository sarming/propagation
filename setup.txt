# Install Miniconda
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh
bash
conda config --set auto_activate_base false

# Development environment
conda env create -n snsim -f environment.yml
conda activate snsim
conda config --env --add channels conda-forge
flit install --symlink

# Production environment with native MPI
conda env create -n snsim-prod -f environment-prod.yml
conda activate snsim-prod
conda remove mpi4py -c conda-forge
pip install mpi4py
